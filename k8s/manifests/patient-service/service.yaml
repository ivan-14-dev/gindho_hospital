apiVersion: v1
kind: Service
metadata:
  name: patient-service-node
  namespace: patient
spec:
  type: NodePort
  selector:
    app: patient-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      nodePort: 31319
