apiVersion: apps/v1
kind: Deployment
metadata:
  name: prescription-service
  namespace: prescription
  labels:
    app: prescription-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: prescription-service
  template:
    metadata:
      labels:
        app: prescription-service
    spec:
      containers:
      - name: prescription-service
        image: gindho/prescription-service:latest
        ports:
        - containerPort: 8090
        env:
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            configMapKeyRef:
              name: prescription-service-config
              key: database_url
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: prescription-service-secret
              key: db_username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: prescription-service-secret
              key: db_password
        - name: KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: prescription-service-config
              key: kafka_servers
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: prescription-service-secret
              key: jwt_secret
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8090
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8090
          initialDelaySeconds: 20
          periodSeconds: 5
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: prescription-service
  namespace: prescription
spec:
  selector:
    app: prescription-service
  ports:
  - port: 8090
    targetPort: 8090
    name: http
  type: ClusterIP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: prescription-service-config
  namespace: prescription
data:
  database_url: "jdbc:postgresql://postgres.prescription.svc.cluster.local:5432/prescription_service"
  kafka_servers: "kafka.infrastructure-namespace.svc.cluster.local:9092"
---
apiVersion: v1
kind: Secret
metadata:
  name: prescription-service-secret
  namespace: prescription
type: Opaque
data:
  db_username: Z2luZGhv
  db_password: Z2luZGhvMTIz
  jwt_secret: NDA0RTYzNTI2NjU1NkE1ODZFMzI3MjM1NzUzODc4MkY0MTNGNDQyODQ3MkI0QjYyNTA2NDUzNjc1NjZCNTk3MA==
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: prescription-service-hpa
  namespace: prescription
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: prescription-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
