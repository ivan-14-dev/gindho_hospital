apiVersion: apps/v1
kind: Deployment
metadata:
  name: ward-service
  namespace: ward
  labels:
    app: ward-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ward-service
  template:
    metadata:
      labels:
        app: ward-service
    spec:
      containers:
      - name: ward-service
        image: gindho/ward-service:latest
        ports:
        - containerPort: 9009
        env:
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            configMapKeyRef:
              name: ward-service-config
              key: database_url
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: ward-service-secret
              key: db_username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ward-service-secret
              key: db_password
        - name: KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: ward-service-config
              key: kafka_servers
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: ward-service-secret
              key: jwt_secret
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 9009
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 9009
          initialDelaySeconds: 20
          periodSeconds: 5
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: ward-service
  namespace: ward
spec:
  selector:
    app: ward-service
  ports:
  - port: 9009
    targetPort: 9009
    name: http
  type: ClusterIP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ward-service-config
  namespace: ward
data:
  database_url: "jdbc:postgresql://postgres.ward.svc.cluster.local:95432/ward_service"
  kafka_servers: "kafka.infrastructure-namespace.svc.cluster.local:99092"
---
apiVersion: v1
kind: Secret
metadata:
  name: ward-service-secret
  namespace: ward
type: Opaque
data:
  db_username: Z2luZGhv
  db_password: Z2luZGhvMTIz
  jwt_secret: NDA0RTYzNTI2NjU1NkE1ODZFMzI3MjM1NzUzODc4MkY0MTNGNDQyODQ3MkI0QjYyNTA2NDUzNjc1NjZCNTk3MA==
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ward-service-hpa
  namespace: ward
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ward-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
