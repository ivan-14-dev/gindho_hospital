apiVersion: apps/v1
kind: Deployment
metadata:
  name: ambulance-service
  namespace: ambulance
  labels:
    app: ambulance-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ambulance-service
  template:
    metadata:
      labels:
        app: ambulance-service
    spec:
      containers:
      - name: ambulance-service
        image: gindho/ambulance-service:latest
        ports:
        - containerPort: 9023
        env:
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            configMapKeyRef:
              name: ambulance-service-config
              key: database_url
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: ambulance-service-secret
              key: db_username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ambulance-service-secret
              key: db_password
        - name: KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: ambulance-service-config
              key: kafka_servers
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: ambulance-service-secret
              key: jwt_secret
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 9023
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 9023
          initialDelaySeconds: 20
          periodSeconds: 5
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: ambulance-service
  namespace: ambulance
spec:
  selector:
    app: ambulance-service
  ports:
  - port: 9023
    targetPort: 9023
    name: http
  type: ClusterIP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ambulance-service-config
  namespace: ambulance
data:
  database_url: "jdbc:postgresql://postgres.ambulance.svc.cluster.local:95432/ambulance_service"
  kafka_servers: "kafka.infrastructure-namespace.svc.cluster.local:99092"
---
apiVersion: v1
kind: Secret
metadata:
  name: ambulance-service-secret
  namespace: ambulance
type: Opaque
data:
  db_username: Z2luZGhv
  db_password: Z2luZGhvMTIz
  jwt_secret: NDA0RTYzNTI2NjU1NkE1ODZFMzI3MjM1NzUzODc4MkY0MTNGNDQyODQ3MkI0QjYyNTA2NDUzNjc1NjZCNTk3MA==
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ambulance-service-hpa
  namespace: ambulance
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ambulance-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
