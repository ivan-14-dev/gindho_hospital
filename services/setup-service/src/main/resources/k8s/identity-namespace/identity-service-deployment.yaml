apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-service
  namespace: identity
  labels:
    app: identity-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: identity-service
  template:
    metadata:
      labels:
        app: identity-service
    spec:
      containers:
      - name: identity-service
        image: gindho/identity-service:latest
        ports:
        - containerPort: 9001
        env:
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            configMapKeyRef:
              name: identity-service-config
              key: database_url
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: identity-service-secret
              key: db_username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: identity-service-secret
              key: db_password
        - name: KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: identity-service-config
              key: kafka_servers
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: identity-service-secret
              key: jwt_secret
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 9001
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 9001
          initialDelaySeconds: 20
          periodSeconds: 5
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: identity
spec:
  selector:
    app: identity-service
  ports:
  - port: 9001
    targetPort: 9001
    name: http
  type: ClusterIP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: identity-service-config
  namespace: identity
data:
  database_url: "jdbc:postgresql://postgres.identity.svc.cluster.local:5432/identity_service"
  kafka_servers: "kafka.infrastructure-namespace.svc.cluster.local:9092"
---
apiVersion: v1
kind: Secret
metadata:
  name: identity-service-secret
  namespace: identity
type: Opaque
data:
  db_username: Z2luZGhv
  db_password: Z2luZGhvMTIz
  jwt_secret: NDA0RTYzNTI2NjU1NkE1ODZFMzI3MjM1NzUzODc4MkY0MTNGNDQyODQ3MkI0QjYyNTA2NDUzNjc1NjZCNTk3MA==
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: identity-service-hpa
  namespace: identity
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: identity-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
