apiVersion: apps/v1
kind: Deployment
metadata:
  name: insurance-service
  namespace: insurance
  labels:
    app: insurance-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: insurance-service
  template:
    metadata:
      labels:
        app: insurance-service
    spec:
      containers:
      - name: insurance-service
        image: gindho/insurance-service:latest
        ports:
        - containerPort: 9018
        env:
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            configMapKeyRef:
              name: insurance-service-config
              key: database_url
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: insurance-service-secret
              key: db_username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: insurance-service-secret
              key: db_password
        - name: KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: insurance-service-config
              key: kafka_servers
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: insurance-service-secret
              key: jwt_secret
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 9018
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 9018
          initialDelaySeconds: 20
          periodSeconds: 5
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: insurance-service
  namespace: insurance
spec:
  selector:
    app: insurance-service
  ports:
  - port: 9018
    targetPort: 9018
    name: http
  type: ClusterIP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: insurance-service-config
  namespace: insurance
data:
  database_url: "jdbc:postgresql://postgres.insurance.svc.cluster.local:5432/insurance_service"
  kafka_servers: "kafka.infrastructure-namespace.svc.cluster.local:9092"
---
apiVersion: v1
kind: Secret
metadata:
  name: insurance-service-secret
  namespace: insurance
type: Opaque
data:
  db_username: Z2luZGhv
  db_password: Z2luZGhvMTIz
  jwt_secret: NDA0RTYzNTI2NjU1NkE1ODZFMzI3MjM1NzUzODc4MkY0MTNGNDQyODQ3MkI0QjYyNTA2NDUzNjc1NjZCNTk3MA==
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: insurance-service-hpa
  namespace: insurance
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: insurance-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
